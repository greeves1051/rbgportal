<p><a href="<%= root_path %>">Back to Home</a></p>
<h1>Brokers</h1>
<button><a href="<%= new_broker_path %>">Create New Broker</a></button>
<ul>
	<% @brokers.each do |broker| %>
		<li><a href="<%= broker_path(broker) %>"><%= broker.name %></a></li>
	<% end %>
</ul>
